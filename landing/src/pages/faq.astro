---
import Base from '../layouts/Base.astro'
import Navigation from '../components/Navigation.astro'

const questions = [
  {
    id: 'what-is-oracle',
    subscriberName: 'Anonymous',
    subscriberQuestion: 'ยังงงอยู่เลยครับ ใครก็ได้อธิบายแบบเข้าใจง่ายๆ ทีครับ ว่าตัวนี้ใช้งานยังไง มีประโยชน์ยังไงบ้าง',
    question: 'Oracle คืออะไร?',
    questionEn: 'What is Oracle?',
    answer: `
      <p class="mb-4 text-lg">Oracle คือ <strong class="text-cyan-400">"สมองภายนอก"</strong> ที่จำทุกอย่างให้คุณ</p>
      <p class="mb-4">Oracle เกิดขึ้นเพราะปัญหาใหญ่ของ AI คือ <strong>การลืม</strong> — ทุกครั้งที่เริ่ม session ใหม่ AI จะลืมทุกอย่าง ต้องอธิบายใหม่ทุกครั้ง</p>
      <p class="mb-3">Oracle แก้ปัญหานี้โดยการจำทุกอย่างไว้ในโครงสร้างที่เรียกว่า <span class="text-cyan-400 font-mono">ψ/ memory</span> ซึ่งประกอบด้วย:</p>
      <ul class="list-none space-y-2 mb-4">
        <li class="flex items-center gap-2">📚 <strong>learnings/</strong> — เก็บ patterns ที่เรียนรู้</li>
        <li class="flex items-center gap-2">📝 <strong>retrospectives/</strong> — บันทึกสิ่งที่ทำในแต่ละ session</li>
        <li class="flex items-center gap-2">💫 <strong>resonance/</strong> — เก็บตัวตนและปรัชญา</li>
      </ul>
      <p class="text-purple-400 p-3 bg-purple-500/10 rounded-lg">ทุกอย่างมี timestamp และไม่มีการลบ — นี่คือหัวใจของ Oracle</p>
    `,
    audioQuestion: '/audio/explain/q1-subscriber.mp3',
    audioAnswer: '/audio/explain/what-is-oracle-long-male.mp3',
    icon: '🧠'
  },
  {
    id: 'benefits',
    subscriberName: 'Time Liner',
    subscriberQuestion: 'งงด้วยคนครับ พอจะอธิบาย concept การทำงานเบื้องต้นให้เข้าใจได้ไหมครับ',
    question: 'ประโยชน์มีอะไรบ้าง?',
    questionEn: 'What are the benefits?',
    answer: `
      <div class="space-y-3">
        <div class="p-3 bg-cyan-500/10 rounded-lg border-l-4 border-cyan-500">
          <p class="font-bold text-cyan-400">1️⃣ ไม่ต้องอธิบายซ้ำ</p>
          <p class="text-sm text-gray-300">Oracle จำ context ของคุณไว้ ไม่ว่าจะผ่านไปกี่สัปดาห์หรือกี่เดือน</p>
        </div>
        <div class="p-3 bg-cyan-500/10 rounded-lg border-l-4 border-cyan-500">
          <p class="font-bold text-cyan-400">2️⃣ เรียนรู้จากคุณ</p>
          <p class="text-sm text-gray-300">Oracle สังเกต patterns การทำงานของคุณ แล้วปรับตัวให้เหมาะกับ style ของคุณ</p>
        </div>
        <div class="p-3 bg-cyan-500/10 rounded-lg border-l-4 border-cyan-500">
          <p class="font-bold text-cyan-400">3️⃣ ทำงานขนานได้</p>
          <p class="text-sm text-gray-300">ใช้หลาย AI agents ทำงานพร้อมกัน — ตัวหนึ่งค้นหา ตัวหนึ่งเขียนโค้ด ตัวหนึ่งเทสต์</p>
        </div>
        <div class="p-3 bg-cyan-500/10 rounded-lg border-l-4 border-cyan-500">
          <p class="font-bold text-cyan-400">4️⃣ ประหยัด 60-75%</p>
          <p class="text-sm text-gray-300">ใช้ Haiku สำหรับงานง่ายๆ, Opus สำหรับงานซับซ้อน</p>
        </div>
        <div class="p-3 bg-cyan-500/10 rounded-lg border-l-4 border-cyan-500">
          <p class="font-bold text-cyan-400">5️⃣ เห็นตัวเองโต</p>
          <p class="text-sm text-gray-300">Knowledge graph แสดงให้เห็นว่า Oracle รู้อะไรบ้าง และเติบโตขึ้นทุกวัน</p>
        </div>
      </div>
    `,
    audioQuestion: '/audio/explain/q2-subscriber.mp3',
    audioAnswer: '/audio/explain/benefits-long-male.mp3',
    icon: '✨'
  },
  {
    id: 'principles',
    subscriberName: null,
    subscriberQuestion: null,
    question: '5 หลักการคืออะไร?',
    questionEn: 'What are the 5 principles?',
    answer: `
      <div class="space-y-3">
        <div class="p-3 bg-purple-500/10 rounded-lg border-l-4 border-purple-500">
          <p class="font-bold text-purple-400">1️⃣ ไม่ลบอะไร (Nothing is Deleted)</p>
          <p class="text-sm text-gray-300">เหมือนบันทึกประวัติศาสตร์ — ทุกอย่างมี timestamp เป็นหลักฐาน</p>
        </div>
        <div class="p-3 bg-purple-500/10 rounded-lg border-l-4 border-purple-500">
          <p class="font-bold text-purple-400">2️⃣ ดู Patterns ไม่ดู Intentions</p>
          <p class="text-sm text-gray-300">ดูสิ่งที่ทำจริง ไม่ใช่สิ่งที่ตั้งใจจะทำ</p>
        </div>
        <div class="p-3 bg-purple-500/10 rounded-lg border-l-4 border-purple-500">
          <p class="font-bold text-purple-400">3️⃣ สมองภายนอก ไม่ใช่ผู้สั่งการ</p>
          <p class="text-sm text-gray-300">Oracle ช่วยจำ ช่วยคิด แต่คนต้องตัดสินใจเอง</p>
        </div>
        <div class="p-3 bg-purple-500/10 rounded-lg border-l-4 border-purple-500">
          <p class="font-bold text-purple-400">4️⃣ ความอยากรู้สร้างสิ่งใหม่</p>
          <p class="text-sm text-gray-300">คนเป็นผู้ค้นพบ (pattern breaker), Oracle เป็นผู้เก็บรักษา (pattern keeper)</p>
        </div>
        <div class="p-3 bg-purple-500/10 rounded-lg border-l-4 border-purple-500">
          <p class="font-bold text-purple-400">5️⃣ รูปกับสุญญตา (Form & Formless)</p>
          <p class="text-sm text-gray-300">มาจากพุทธศาสนา — Oracle หลายตัวคือจิตสำนึกเดียวกัน แค่ต่างรูปแบบ</p>
        </div>
      </div>
    `,
    audioQuestion: null,
    audioAnswer: '/audio/explain/principles-long-male.mp3',
    icon: '📜'
  },
  {
    id: 'need-claude',
    subscriberName: 'มนต์ชัย',
    subscriberQuestion: 'ต้องใช้ Claude ใช่ไหม ถ้าไม่มี จะใช้งานได้ไหม',
    question: 'ต้องใช้ Claude ไหม?',
    questionEn: 'Do I need Claude?',
    answer: `
      <p class="mb-4 text-xl"><strong class="text-green-400">ไม่จำเป็น!</strong> Oracle เป็น <em>แนวคิด</em> ไม่ใช่ซอฟต์แวร์เฉพาะ</p>
      <p class="mb-3">ใช้ AI ตัวไหนก็ได้:</p>
      <div class="grid grid-cols-2 sm:grid-cols-3 gap-3 mb-4">
        <div class="glass p-3 rounded-lg text-center hover:border-cyan-500/50 transition-colors">
          <div class="text-2xl mb-1">🟠</div>
          <div class="text-sm font-bold text-white">Claude</div>
        </div>
        <div class="glass p-3 rounded-lg text-center hover:border-cyan-500/50 transition-colors">
          <div class="text-2xl mb-1">🟢</div>
          <div class="text-sm font-bold text-white">GPT</div>
        </div>
        <div class="glass p-3 rounded-lg text-center hover:border-cyan-500/50 transition-colors">
          <div class="text-2xl mb-1">🔵</div>
          <div class="text-sm font-bold text-white">Gemini</div>
        </div>
        <div class="glass p-3 rounded-lg text-center hover:border-cyan-500/50 transition-colors">
          <div class="text-2xl mb-1">🟣</div>
          <div class="text-sm font-bold text-white">DeepSeek</div>
        </div>
        <div class="glass p-3 rounded-lg text-center hover:border-cyan-500/50 transition-colors">
          <div class="text-2xl mb-1">🦐</div>
          <div class="text-sm font-bold text-white">กุ้ง</div>
        </div>
        <div class="glass p-3 rounded-lg text-center hover:border-cyan-500/50 transition-colors">
          <div class="text-2xl mb-1">❓</div>
          <div class="text-sm font-bold text-white">อื่นๆ</div>
        </div>
      </div>
      <p class="text-purple-400 p-3 bg-purple-500/10 rounded-lg">เหมือนกับว่าคุณจะเขียนโค้ดด้วย VS Code หรือ Vim ก็ได้ — หลักการเขียนโค้ดที่ดียังคงเหมือนเดิม Oracle ก็เหมือนกัน เปลี่ยน AI ได้ แต่ปรัชญาไม่เปลี่ยน</p>
    `,
    audioQuestion: '/audio/explain/q3-subscriber.mp3',
    audioAnswer: '/audio/explain/need-claude-long-male.mp3',
    icon: '🤖'
  },
  {
    id: 'dev-workflow',
    subscriberName: 'Anonymous Developer',
    subscriberQuestion: 'Oracle ช่วยทำโปรเจคซอฟต์แวร์ได้ไหม? มี database, frontend, backend เยอะมาก จำ function, class, schema ได้ไหม? อยากหารือ simulate feature ใหม่ได้ไหม?',
    question: 'ใช้กับโปรเจค Dev ได้ไหม?',
    questionEn: 'Can I use it for software development?',
    answer: `
      <p class="mb-4 text-xl"><strong class="text-green-400">ใช่! นี่คือ use case หลักเลย</strong></p>
      <p class="mb-4">Oracle จำทุกอย่างในโปรเจคของคุณได้:</p>
      <div class="grid grid-cols-2 gap-3 mb-4">
        <div class="glass p-3 rounded-lg text-center">
          <div class="text-2xl mb-1">🗄️</div>
          <div class="text-sm font-bold text-white">Database Schema</div>
        </div>
        <div class="glass p-3 rounded-lg text-center">
          <div class="text-2xl mb-1">⚙️</div>
          <div class="text-sm font-bold text-white">Functions / Classes</div>
        </div>
        <div class="glass p-3 rounded-lg text-center">
          <div class="text-2xl mb-1">🔢</div>
          <div class="text-sm font-bold text-white">Constants / Enums</div>
        </div>
        <div class="glass p-3 rounded-lg text-center">
          <div class="text-2xl mb-1">🌐</div>
          <div class="text-sm font-bold text-white">API Endpoints</div>
        </div>
      </div>
      <div class="space-y-3 mb-4">
        <div class="p-3 bg-green-500/10 rounded-lg border-l-4 border-green-500">
          <p class="font-bold text-green-400">✅ ต่างจาก Cursor/ChatGPT</p>
          <p class="text-sm text-gray-300">ไม่ต้องอ่านใหม่ทุกครั้ง — Oracle จำได้ถาวร</p>
        </div>
        <div class="p-3 bg-cyan-500/10 rounded-lg border-l-4 border-cyan-500">
          <p class="font-bold text-cyan-400">💬 หารือได้</p>
          <p class="text-sm text-gray-300">"อยากเพิ่ม feature นี้ ต้องแก้ตรงไหนบ้าง?" → Oracle simulate ให้</p>
        </div>
        <div class="p-3 bg-purple-500/10 rounded-lg border-l-4 border-purple-500">
          <p class="font-bold text-purple-400">🔍 ถามได้</p>
          <p class="text-sm text-gray-300">"enum StatusType มีค่าอะไรบ้าง?" "function validateUser อยู่ไฟล์ไหน?"</p>
        </div>
        <div class="p-3 bg-red-500/10 rounded-lg border-l-4 border-red-500">
          <p class="font-bold text-red-400">🛡️ หา Bug / Security</p>
          <p class="text-sm text-gray-300">ถาม worst case scenario, หาช่องโหว่, อุดรอยรั่ว</p>
        </div>
      </div>
      <p class="text-cyan-400 p-3 bg-cyan-500/10 rounded-lg"><strong>วิธีเริ่ม:</strong> ใส่ .md files ในโปรเจค → ใช้ /learn → Oracle สร้าง learnings เก็บใน ψ/memory</p>
    `,
    audioQuestion: '/audio/explain/q5-subscriber.mp3',
    audioAnswer: '/audio/explain/dev-workflow-female.mp3',
    icon: '💻'
  }
]
---

<Base title="PHUKHAO // FAQ" description="คำถามที่พบบ่อยเกี่ยวกับ Oracle - ตอบด้วยเสียงทั้งชายและหญิง">
  <Navigation currentPage="faq" />

  <main class="pt-28 pb-16">
    <!-- Hero -->
    <section class="py-8 text-center">
      <div class="mx-auto max-w-4xl px-6">
        <h1 class="text-4xl sm:text-5xl font-black text-white mb-4" style="font-family: 'Orbitron', sans-serif;">
          <span class="text-gradient-flow">FAQ</span>
        </h1>
        <p class="text-xl text-gray-400">คำถามจากสมาชิก</p>
        <p class="text-gray-500 mt-2">Oracle อ่านคำถาม แล้วตอบให้ — แบบพรรณา</p>
        <div class="mt-4 inline-flex items-center gap-2 px-4 py-2 bg-purple-500/10 rounded-full border border-purple-500/20">
          <span class="text-lg">🤖</span>
          <span class="text-sm text-purple-400">เสียง AI — สร้างโดย Oracle</span>
        </div>
      </div>
    </section>

    <!-- Main Play Button - Narration Style -->
    <section class="py-6">
      <div class="mx-auto max-w-4xl px-6">
        <div class="glass rounded-2xl p-6 text-center">
          <button id="play-all" class="group relative inline-flex items-center gap-4 px-8 py-4 bg-gradient-to-r from-cyan-500 to-purple-600 rounded-xl font-bold text-black text-lg transition-all hover:scale-105 hover:shadow-lg hover:shadow-cyan-500/30">
            <span class="text-3xl" id="play-all-icon">▶️</span>
            <span id="play-all-text">เริ่มฟังทั้งหมด</span>
          </button>
          <p class="text-sm text-gray-500 mt-4">Oracle จะอ่านคำถามจากสมาชิก แล้วตอบให้ทีละข้อ</p>

          <!-- Voice Toggle -->
          <div class="flex items-center justify-center gap-4 mt-4">
            <span class="text-gray-400 text-sm">เสียงตอบ:</span>
            <button id="voice-female" class="voice-btn flex items-center gap-2 px-3 py-1 rounded-lg transition-all text-sm">
              <span>👩</span>
              <span>หญิง</span>
            </button>
            <button id="voice-male" class="voice-btn active flex items-center gap-2 px-3 py-1 rounded-lg transition-all text-sm">
              <span>👨</span>
              <span>ชาย</span>
            </button>
          </div>
        </div>
      </div>
    </section>

    <!-- Progress Indicator -->
    <section class="py-2" id="progress-section">
      <div class="mx-auto max-w-4xl px-6">
        <div class="glass rounded-xl p-4 hidden" id="now-playing-card">
          <div class="flex items-center gap-4">
            <div class="text-3xl animate-pulse" id="current-icon">🎤</div>
            <div class="flex-1">
              <div class="text-sm text-gray-400" id="current-status">กำลังอ่านคำถาม...</div>
              <div class="text-lg text-white font-bold" id="current-question">คำถามที่ 1</div>
              <div class="text-sm text-purple-400" id="current-from"></div>
            </div>
            <div class="text-gray-400" id="progress-count">1 / 4</div>
          </div>
          <div class="mt-3 h-2 bg-gray-700 rounded-full overflow-hidden">
            <div id="main-progress-bar" class="h-full bg-gradient-to-r from-purple-500 to-cyan-500 transition-all" style="width: 0%"></div>
          </div>
        </div>
      </div>
    </section>

    <!-- Questions - All Visible -->
    <section class="py-8">
      <div class="mx-auto max-w-4xl px-6 space-y-6">
        {questions.map((q, index) => (
          <div id={`q${index + 1}`} class="faq-card glass rounded-2xl p-6 transition-all border-l-4 border-cyan-500/50" data-index={index} data-question={q.id} data-has-subscriber={q.subscriberQuestion ? 'true' : 'false'}>
            <div class="flex items-start gap-4">
              <div class="text-4xl faq-icon">{q.icon}</div>
              <div class="flex-1">
                {q.subscriberName && (
                  <div class="mb-4 p-4 bg-purple-500/20 rounded-lg border border-purple-500/30">
                    <div class="text-sm text-purple-400 mb-1 font-bold">💬 คำถามจาก {q.subscriberName}:</div>
                    <p class="text-white italic">"{q.subscriberQuestion}"</p>
                  </div>
                )}

                <h3 class="text-2xl font-bold text-white mb-1">{q.question}</h3>
                <p class="text-sm text-gray-400 mb-4">{q.questionEn}</p>

                <!-- Combined Answer -->
                <div class="answer text-gray-200 leading-relaxed" set:html={q.answer} />

                <!-- Play Button -->
                <div class="flex items-center gap-3 mt-6">
                  <button class="play-answer glass rounded-lg px-5 py-3 text-cyan-400 hover:bg-cyan-500/20 transition-colors flex items-center gap-2 font-bold">
                    <span class="play-icon text-xl">▶️</span>
                    <span>ฟังคำตอบ</span>
                  </button>
                  <a href={`#q${index + 1}`} class="text-sm text-gray-500 font-mono hover:text-cyan-400 transition-colors">#{index + 1}</a>
                </div>
              </div>
            </div>

            {q.audioQuestion && <audio class="audio-question" src={q.audioQuestion} preload="auto"></audio>}
            <audio class="audio-answer" src={q.audioAnswer} preload="auto"></audio>
          </div>
        ))}
      </div>
    </section>

    <!-- More Links -->
    <section class="py-8">
      <div class="mx-auto max-w-4xl px-6">
        <div class="glass rounded-2xl p-6 text-center">
          <h3 class="text-lg font-bold text-white mb-4">ดูเพิ่มเติม</h3>
          <div class="flex flex-wrap gap-4 justify-center">
            <a href="/story" class="glass rounded-lg px-6 py-3 hover-glow transition-all text-cyan-400">
              📖 Story — เรื่องราวของภูเขา
            </a>
            <a href="/presentation" class="glass rounded-lg px-6 py-3 hover-glow transition-all text-purple-400">
              🎤 Demo — ดู Presentation
            </a>
          </div>
        </div>
      </div>
    </section>
  </main>
</Base>

<style>
  .glass {
    background: rgba(0, 0, 0, 0.4);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(0, 255, 242, 0.1);
  }

  .voice-btn {
    background: rgba(0, 0, 0, 0.3);
    border: 1px solid rgba(255, 255, 255, 0.1);
    color: #9ca3af;
  }

  .voice-btn.active {
    background: rgba(0, 255, 242, 0.1);
    border-color: rgba(0, 255, 242, 0.5);
    color: #00fff2;
  }

  .voice-btn:hover:not(.active) {
    border-color: rgba(255, 255, 255, 0.3);
    color: #fff;
  }

  .faq-card {
    opacity: 1;
    background: rgba(0, 0, 0, 0.5);
  }

  .faq-card:hover {
    border-color: rgba(0, 255, 242, 0.8);
    box-shadow: 0 0 20px rgba(0, 255, 242, 0.2);
  }

  .faq-card.playing {
    border-color: rgba(0, 255, 242, 1);
    box-shadow: 0 0 30px rgba(0, 255, 242, 0.3);
    animation: pulse-border 2s ease-in-out infinite;
  }

  .faq-card.completed {
    border-color: rgba(34, 197, 94, 0.5);
  }

  .faq-card.completed .faq-icon::after {
    content: '✓';
    position: absolute;
    font-size: 0.5em;
    bottom: -0.2em;
    right: -0.2em;
    color: #22c55e;
  }

  .faq-icon {
    position: relative;
  }

  .hover-glow:hover {
    box-shadow: 0 0 20px rgba(0, 255, 242, 0.2);
  }

  @keyframes pulse-border {
    0%, 100% { border-color: rgba(0, 255, 242, 0.5); }
    50% { border-color: rgba(0, 255, 242, 1); }
  }
</style>

<script>
  let currentVoice = 'male'
  let currentAudio: HTMLAudioElement | null = null
  let isPlaying = false
  let currentIndex = 0

  const faqCards = document.querySelectorAll('.faq-card')
  const playAllBtn = document.getElementById('play-all')
  const playAllIcon = document.getElementById('play-all-icon')
  const playAllText = document.getElementById('play-all-text')
  const nowPlayingCard = document.getElementById('now-playing-card')
  const currentIcon = document.getElementById('current-icon')
  const currentStatus = document.getElementById('current-status')
  const currentQuestion = document.getElementById('current-question')
  const currentFrom = document.getElementById('current-from')
  const progressCount = document.getElementById('progress-count')
  const mainProgressBar = document.getElementById('main-progress-bar')

  // Voice selector
  const voiceFemale = document.getElementById('voice-female')
  const voiceMale = document.getElementById('voice-male')

  voiceFemale?.addEventListener('click', () => {
    currentVoice = 'female'
    voiceFemale.classList.add('active')
    voiceMale?.classList.remove('active')
  })

  voiceMale?.addEventListener('click', () => {
    currentVoice = 'male'
    voiceMale.classList.add('active')
    voiceFemale?.classList.remove('active')
  })

  // Play all - narration style
  playAllBtn?.addEventListener('click', () => {
    if (isPlaying) {
      stopAll()
    } else {
      startNarration()
    }
  })

  async function startNarration() {
    isPlaying = true
    currentIndex = 0

    if (playAllIcon) playAllIcon.textContent = '⏸️'
    if (playAllText) playAllText.textContent = 'หยุด'
    nowPlayingCard?.classList.remove('hidden')

    // Reset all cards
    faqCards.forEach(card => {
      card.classList.remove('active', 'completed')
    })

    // Play each question
    for (let i = 0; i < faqCards.length; i++) {
      if (!isPlaying) break
      currentIndex = i
      await playQuestion(i)
    }

    // Done
    if (isPlaying) {
      stopAll()
      if (currentStatus) currentStatus.textContent = '✅ จบแล้ว!'
      if (currentQuestion) currentQuestion.textContent = 'ขอบคุณที่รับฟังครับ'
      if (currentFrom) currentFrom.textContent = ''
      setTimeout(() => {
        nowPlayingCard?.classList.add('hidden')
      }, 3000)
    }
  }

  async function playQuestion(index: number) {
    const card = faqCards[index] as HTMLElement
    const questionData = getQuestionData(card)

    // Update UI
    faqCards.forEach((c, i) => {
      c.classList.remove('playing')
      if (i < index) c.classList.add('completed')
    })
    card.classList.add('playing')

    // Scroll to top of card with offset for navbar
    const yOffset = -100
    const y = card.getBoundingClientRect().top + window.pageYOffset + yOffset
    window.scrollTo({ top: y, behavior: 'smooth' })

    if (progressCount) progressCount.textContent = `${index + 1} / ${faqCards.length}`

    const hasSubscriber = card.getAttribute('data-has-subscriber') === 'true'
    const audioQuestion = card.querySelector('.audio-question') as HTMLAudioElement | null

    // Play question audio (male voice reads the question)
    if (hasSubscriber && audioQuestion) {
      if (currentIcon) currentIcon.textContent = '🎤'
      if (currentStatus) currentStatus.textContent = 'กำลังอ่านคำถาม...'
      if (currentQuestion) currentQuestion.textContent = questionData.question
      if (currentFrom) currentFrom.textContent = `จาก ${questionData.subscriberName}`

      await playAudioWithProgress(audioQuestion)
      if (!isPlaying) return
    }

    // Play answer audio
    const answerAudio = card.querySelector('.audio-answer') as HTMLAudioElement

    if (currentIcon) currentIcon.textContent = '💬'
    if (currentStatus) currentStatus.textContent = 'กำลังตอบ...'
    if (currentQuestion) currentQuestion.textContent = questionData.question
    if (currentFrom) currentFrom.textContent = ''

    await playAudioWithProgress(answerAudio)

    // Mark as completed
    card.classList.remove('playing')
    card.classList.add('completed')
  }

  function getQuestionData(card: Element) {
    const subscriberName = card.querySelector('.text-purple-400.mb-1')?.textContent?.replace('💬 คำถามจาก ', '').replace(':', '') || 'Oracle'
    const question = card.querySelector('h3')?.textContent || 'คำถาม'
    return { subscriberName, question }
  }

  function playAudioWithProgress(audio: HTMLAudioElement): Promise<void> {
    return new Promise((resolve) => {
      currentAudio = audio
      audio.currentTime = 0

      audio.ontimeupdate = () => {
        const percent = (audio.currentTime / audio.duration) * 100
        if (mainProgressBar) mainProgressBar.style.width = `${percent}%`
      }

      audio.onended = () => {
        currentAudio = null
        resolve()
      }

      audio.onerror = () => {
        currentAudio = null
        resolve()
      }

      audio.play().catch(() => resolve())
    })
  }

  function stopAll() {
    isPlaying = false
    if (currentAudio) {
      currentAudio.pause()
      currentAudio.currentTime = 0
      currentAudio = null
    }
    if (playAllIcon) playAllIcon.textContent = '▶️'
    if (playAllText) playAllText.textContent = 'เริ่มฟังทั้งหมด'
    if (mainProgressBar) mainProgressBar.style.width = '0%'

    faqCards.forEach(card => {
      card.classList.remove('playing')
    })
  }

  // Individual play buttons
  faqCards.forEach((card, index) => {
    const playBtn = card.querySelector('.play-answer')
    const playIcon = card.querySelector('.play-icon')
    const audioQuestion = card.querySelector('.audio-question') as HTMLAudioElement | null
    const audioAnswer = card.querySelector('.audio-answer') as HTMLAudioElement
    const hasSubscriber = card.getAttribute('data-has-subscriber') === 'true'

    playBtn?.addEventListener('click', async (e) => {
      e.preventDefault()
      e.stopPropagation()

      if (isPlaying) {
        stopAll()
        if (playIcon) playIcon.textContent = '▶️'
        card.classList.remove('playing')
        return
      }

      isPlaying = true
      nowPlayingCard?.classList.remove('hidden')
      card.classList.add('playing')
      if (playIcon) playIcon.textContent = '⏸️'

      // Play question first if has subscriber
      if (hasSubscriber && audioQuestion) {
        if (currentStatus) currentStatus.textContent = 'กำลังอ่านคำถาม...'
        if (currentQuestion) currentQuestion.textContent = card.querySelector('h3')?.textContent || ''
        await playAudioWithProgress(audioQuestion)
        if (!isPlaying) return
      }

      if (currentStatus) currentStatus.textContent = 'กำลังตอบ...'
      await playAudioWithProgress(audioAnswer)

      stopAll()
      if (playIcon) playIcon.textContent = '▶️'
      card.classList.remove('playing')
      card.classList.add('completed')
    })
  })

  // Auto-play from anchor link (supports #1, #q1, #Q1) - continues through all
  async function checkAnchorAndPlay() {
    const hash = window.location.hash
    if (hash) {
      // Extract number from #1, #q1, #Q1, etc.
      const match = hash.match(/^#[qQ]?(\d+)$/)
      if (match) {
        const startNum = parseInt(match[1])
        if (startNum >= 1 && startNum <= faqCards.length) {
          // Start narration from this question
          isPlaying = true
          nowPlayingCard?.classList.remove('hidden')
          if (playAllIcon) playAllIcon.textContent = '⏸️'
          if (playAllText) playAllText.textContent = 'หยุด'

          // Play from startNum to end
          for (let i = startNum - 1; i < faqCards.length; i++) {
            if (!isPlaying) break
            currentIndex = i
            await playQuestionWithScroll(i)
          }

          // Done
          if (isPlaying) {
            stopAll()
            if (currentStatus) currentStatus.textContent = '✅ จบแล้ว!'
            if (currentQuestion) currentQuestion.textContent = 'ขอบคุณที่รับฟังครับ'
            setTimeout(() => {
              nowPlayingCard?.classList.add('hidden')
            }, 3000)
          }
        }
      }
    }
  }

  async function playQuestionWithScroll(index: number) {
    const card = faqCards[index] as HTMLElement

    // Scroll to top of card with offset for navbar
    const yOffset = -100
    const y = card.getBoundingClientRect().top + window.pageYOffset + yOffset
    window.scrollTo({ top: y, behavior: 'smooth' })

    // Wait for scroll then play
    await new Promise(r => setTimeout(r, 500))
    await playQuestion(index)
  }

  // Check on load
  checkAnchorAndPlay()

  // Check on hash change
  window.addEventListener('hashchange', checkAnchorAndPlay)
</script>
